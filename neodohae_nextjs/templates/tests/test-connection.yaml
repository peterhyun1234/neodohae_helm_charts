apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "neodohae_nextjs.fullname" . }}-test-connection"
  labels:
    {{- include "neodohae_nextjs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "neodohae_nextjs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
