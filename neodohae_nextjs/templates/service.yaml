apiVersion: v1
kind: Service
metadata:
  name: {{ include "neodohae_nextjs.fullname" . }}
  labels:
    {{- include "neodohae_nextjs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "neodohae_nextjs.selectorLabels" . | nindent 4 }}
