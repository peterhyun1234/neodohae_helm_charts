apiVersion: v1
kind: Service
metadata:
  name: {{ include "neodohae_common.fullname" . }}
  labels:
    {{- include "neodohae_common.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "neodohae_common.selectorLabels" . | nindent 4 }}
