apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "neodohae_nestjs.fullname" . }}-test-connection"
  labels:
    {{- include "neodohae_nestjs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "neodohae_nestjs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
